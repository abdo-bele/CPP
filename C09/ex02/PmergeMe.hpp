#ifndef PMERGEME_HPP
#define PMERGEME_HPP


#include <iostream>
#include <deque>
#include <sstream>
#include <utility>
#include <vector>
#include <ctime>

void    ft_sort(const std::string &obj);
void    ft_sort_deque(std::deque<int> &dsort);
void    ft_sort_vector(std::vector<int> &dsort);


#endif